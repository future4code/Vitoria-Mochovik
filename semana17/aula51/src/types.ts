export type user = {
    id: string
    name: string
    nickname: string
    email: string
    address: string
 }

 export type addressInfo = {
    street: string,
    neighborhood: string,
    city: string,
    state: string
 }

 export type userAddressInfo = {
    id: string,
    zip_code: string,
    street: string,
    number: string,
    complement: string,
    neighborhood: string,
    city: string,
    state: string,
    user_id: string
 }

 